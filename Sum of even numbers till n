Given a number N, print sum of all even numbers from 1 to N.

c++ program 

/////////////

#include<iostream>
using namespace std;


int main(){

       	int n;
        cin>>n;
        int nextnum=1,sum=0;
        while(nextnum<=n){
            if(nextnum%2==0){
                sum=sum+nextnum;
            }
            nextnum = nextnum +1;
        }
        
        cout<<sum;
}
